process FOURTH = 
{
  untracked PSet maxEvents = { untracked int32 input = 100 }
  include "FWCore/Framework/test/cmsExceptionsFatal.cff"

  source = PoolSource 
  {
    untracked vstring fileNames = { "file:testEventHistory_3.root" }
  }

  # Why does the filter module (from step 3) pass 56 events, when I
  # give it a rate of 55%?
  module historytest = HistoryAnalyzer { int32 pass = 4 int32 expectedCount = 56 }
  path p = { historytest }
}
