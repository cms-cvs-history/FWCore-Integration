process test= {
  untracked PSet maxEvents = {untracked int32 input = 20}
  include "FWCore/Framework/test/cmsExceptionsFatal.cff"
  path p ={tester}
  module tester = UseValueExampleAnalyzer{}
  service = ValueExample{ int32 value=2 }

  source = EmptySource {}
}
