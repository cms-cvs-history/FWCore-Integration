# Configuration file for RefTest_t

process TEST = {

	path p = {OtherThing}

	module OtherThing = OtherThingAnalyzer {untracked int32 debugLevel = 1}

	source = PoolInputService {
		untracked string fileName = 'RefTest.root'
		untracked int32 maxEvents = -1
	}

}
